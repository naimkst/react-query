query locations{
  locations{
    results{
      id
      name
      dimension
      residents{
        id
        name
        status
        species
        type
        gender
        image
      }
    }
  }
}